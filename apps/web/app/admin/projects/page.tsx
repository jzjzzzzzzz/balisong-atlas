import Link from "next/link";
import { ArrowRight, Plus } from "lucide-react";
import { AdminSection } from "@/components/AdminSection";
import { StatusPill } from "@/components/StatusPill";
const projects=[["Balisong Atlas Demo Collection","balisong-atlas-demo","published","3 sources · 2 accepted claims"],["Between Two Handles: A Visual History of the Balisong","between-two-handles","draft","Empty research structure"]];
export default function Page(){return <AdminSection eyebrow="Research projects" title="Projects" description="Create and manage evidence collections. Publication is a validated state transition, not a visibility checkbox." action={<Link href="/admin/projects/new" className="inline-flex items-center gap-2 rounded-full bg-ink px-4 py-2.5 text-xs font-bold text-paper"><Plus size={14}/> New project</Link>}><div className="grid gap-4">{projects.map(([title,slug,status,meta])=><Link href={`/admin/projects/${slug==="balisong-atlas-demo"?"demo":slug}`} key={slug} className="group flex flex-wrap items-center justify-between gap-5 rounded-2xl border border-ink/10 bg-white/50 p-6 no-underline"><div><div className="mb-3"><StatusPill>{status}</StatusPill></div><h2 className="font-display text-3xl">{title}</h2><p className="mt-2 text-xs text-quiet">/{slug} · {meta}</p></div><ArrowRight className="transition group-hover:translate-x-1"/></Link>)}</div></AdminSection>}
