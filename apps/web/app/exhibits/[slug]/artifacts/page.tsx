import Link from "next/link";
import { ArrowRight } from "lucide-react";
import { PublicSection } from "@/features/exhibits/PublicSection";
import { StatusPill } from "@/components/StatusPill";
export default async function Page({params}:{params:Promise<{slug:string}>}) { const {slug}=await params; return <PublicSection slug={slug} eyebrow="Collection records" title="Artifacts & design hypotheses" intro="A record may represent a documented object, a design type, or a bounded reconstruction subject. Exact technical fields are intentionally absent.">{slug==="balisong-atlas-demo"?<Link href={`/exhibits/${slug}/artifacts/fictional-kinetic-folding-artifact-a-01`} className="group block rounded-3xl border border-ink/10 bg-white/50 p-7 no-underline"><StatusPill>verified</StatusPill><h2 className="mt-8 font-display text-4xl">Fictional Kinetic Folding Artifact A-01</h2><p className="mt-3 max-w-2xl text-quiet">A unitless abstract fixture used solely to validate evidence review and the safe museum visualization workflow.</p><span className="mt-7 inline-flex items-center gap-2 text-sm font-bold">Open record <ArrowRight size={15} className="transition group-hover:translate-x-1"/></span></Link>:<div className="rounded-3xl border border-dashed border-ink/20 p-12 text-center text-quiet">No reviewed artifact records have been published.</div>}</PublicSection>; }
