import { PublicSection } from "@/features/exhibits/PublicSection";
import { LiteratureRegister } from "@/features/exhibits/LiteratureRegister";
const rows=[["Fictional A-01 research sheet","PDF · Tier A","Public domain","Page-level text and block locations"],["Abstract fixture study 1","Image · Tier A","Public domain","Observed color and contour"],["Abstract fixture study 2","Image · Tier A","Public domain","Observed surface motif"]];
export default async function Page({params}:{params:Promise<{slug:string}>}) { const {slug}=await params; const demo=slug==="balisong-atlas-demo"; return <PublicSection slug={slug} eyebrow={demo?"Source register":"Reading Room · 研究阅览室"} title={demo?"Sources, rights & attribution":"Books before posts."} intro={demo?"The public register prioritizes source identity, location, rights, and contribution over long copied excerpts.":"A ranked, public-safe research register. Source format influences discovery priority, but never determines whether a historical claim is true."}>{demo?<div className="overflow-x-auto border-y border-ink"><table className="w-full min-w-[720px] text-left text-sm"><thead><tr>{["Source","Type & tier","Rights","Research contribution"].map(x=><th key={x} className="border-b border-ink p-4 font-mono text-[9px] uppercase tracking-[.13em]">{x}</th>)}</tr></thead><tbody>{rows.map(row=><tr key={row[0]}>{row.map(cell=><td key={cell} className="border-b border-ink/20 p-4">{cell}</td>)}</tr>)}</tbody></table></div>:<><LiteratureRegister/><div className="mt-10 grid gap-8 border-y border-ink py-7 lg:grid-cols-2"><div><h2 className="font-display text-3xl">Access is not publication.</h2><p className="mt-3 text-sm leading-6 text-quiet">Research copies are stored privately by SHA-256 and excluded from AI, embeddings, public search, and exhibit display until rights and sensitive-content review is complete.</p></div><div><h2 className="font-display text-3xl">Metadata is not evidence.</h2><p className="mt-3 text-sm leading-6 text-quiet">A catalogue lead or web transcription may locate a source. A claim still needs a reviewer-verified passage, page, region, or institutional record.</p></div></div></>}</PublicSection>; }
